#include <bits/stdc++.h>
// #include <D:\C++\debug.h>
using namespace std;
#define codestein                     \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);                    \
    cout.tie(NULL);
#define test   \
    int _t;    \
    cin >> _t; \
    for (int t = 1; t <= _t; t++)
#define be(x) x.begin(), x.end()
#define nl cout << "\n"
#define int long long

void solve()
{
    int n, flag = 0;
    cin >> n;

    nl;
}

signed main()
{
    codestein { solve(); }
}
